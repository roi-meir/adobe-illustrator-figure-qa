{
  "_comment": "Copy to ~/.figure-qa/config.json and edit. All fields are optional — they override the journal preset.",
  "journal": "default",
  "allowedFontSizes": [5, 6, 7, 8],
  "allowedFontFamilies": ["Arial", "Arial MT"],
  "checkEnabled": {
    "fontSize": true,
    "fontFamily": true,
    "scale": true,
    "lowResRaster": true
  }
}
